export const metadata = {
  title: 'Log in | Igris Radar',
  description: 'Log in to your Igris Radar account to run security, SEO, AEO, GEO, brand visibility, and site health audits.',
  alternates: { canonical: '/login' },
  robots: { index: false, follow: false },
};

export default function LoginLayout({ children }) {
  return children;
}
