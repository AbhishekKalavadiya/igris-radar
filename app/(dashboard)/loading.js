import ScreenLoader from '@/components/ui/ScreenLoader';

/**
 * Lightweight loading state shown instantly while any dashboard page
 * is being compiled or fetched.
 */
export default function DashboardLoading() {
  return <ScreenLoader />;
}
